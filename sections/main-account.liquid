{{ 'account.css' | asset_url | stylesheet_tag: preload: true }}

{% comment %}--- TIER LOGIC (unchanged) ---{% endcomment %}
{%- liquid
  assign total_spent = customer.metafields.custom.total_spent | default: 0 | plus: 0
  assign points = customer.metafields.rewards.points | default: 0

  assign tier_1_name = settings.tier_1_name
  assign tier_1_tag = settings.tier_1_tag
  assign tier_1_threshold = settings.tier_1_threshold | plus: 0
  assign tier_2_name = settings.tier_2_name
  assign tier_2_tag = settings.tier_2_tag
  assign tier_2_threshold = settings.tier_2_threshold | plus: 0
  assign tier_3_name = settings.tier_3_name
  assign tier_3_tag = settings.tier_3_tag
  assign tier_3_threshold = settings.tier_3_threshold | plus: 0
  assign tier_4_name = settings.tier_4_name
  assign tier_4_tag = settings.tier_4_tag
  assign tier_4_threshold = settings.tier_4_threshold | plus: 0
  assign tier_5_name = settings.tier_5_name
  assign tier_5_tag = settings.tier_5_tag
  assign tier_5_threshold = settings.tier_5_threshold | plus: 0
  assign tier_6_name = settings.tier_6_name
  assign tier_6_tag = settings.tier_6_tag
  assign tier_6_threshold = settings.tier_6_threshold | plus: 0
  assign prioritize_tags = settings.tier_prioritize_tags | default: true

  assign tier = ""
  if prioritize_tags
    if tier_1_tag != blank and customer.tags contains tier_1_tag
      assign tier = tier_1_name
    elsif tier_2_tag != blank and customer.tags contains tier_2_tag
      assign tier = tier_2_name
    elsif tier_3_tag != blank and customer.tags contains tier_3_tag
      assign tier = tier_3_name
    elsif tier_4_tag != blank and customer.tags contains tier_4_tag
      assign tier = tier_4_name
    elsif tier_5_tag != blank and customer.tags contains tier_5_tag
      assign tier = tier_5_name
    elsif tier_6_tag != blank and customer.tags contains tier_6_tag
      assign tier = tier_6_name
    endif
  endif

  if tier == ""
    if total_spent >= tier_1_threshold
      assign tier = tier_1_name
    elsif total_spent >= tier_2_threshold
      assign tier = tier_2_name
    elsif total_spent >= tier_3_threshold
      assign tier = tier_3_name
    elsif total_spent >= tier_4_threshold
      assign tier = tier_4_name
    elsif total_spent >= tier_5_threshold
      assign tier = tier_5_name
    elsif total_spent >= tier_6_threshold
      assign tier = tier_6_name
    else
      assign tier = tier_6_name
    endif
  endif

  if tier == tier_6_name
    assign next_tier = tier_5_name
    assign next_tier_amount = tier_5_threshold
  elsif tier == tier_5_name
    assign next_tier = tier_4_name
    assign next_tier_amount = tier_4_threshold
  elsif tier == tier_4_name
    assign next_tier = tier_3_name
    assign next_tier_amount = tier_3_threshold
  elsif tier == tier_3_name
    assign next_tier = tier_2_name
    assign next_tier_amount = tier_2_threshold
  elsif tier == tier_2_name
    assign next_tier = tier_1_name
    assign next_tier_amount = tier_1_threshold
  elsif tier == tier_1_name
    assign next_tier = "MAX LEVEL"
    assign next_tier_amount = total_spent
  else
    assign next_tier = tier_6_name
    assign next_tier_amount = tier_6_threshold
    if next_tier_amount == 0
       assign next_tier = tier_5_name
       assign next_tier_amount = tier_5_threshold
    endif
  endif

  if next_tier == "MAX LEVEL"
    assign progress_percentage = 100
    assign remaining_amount = 0
  else
    if next_tier_amount > 0
      assign progress_percentage = total_spent | times: 100 | divided_by: next_tier_amount
    else
      assign progress_percentage = 0
    endif
    assign remaining_amount = next_tier_amount | minus: total_spent
  endif
-%}

{% comment %}--- FORMAT NUMBERS ---{% endcomment %}
{%- liquid
  assign fmt_total_spent = total_spent | floor | split: '' | reverse
  assign fmt_ts = ''
  for digit in fmt_total_spent
    assign r = forloop.index0 | modulo: 3
    if r == 0 and forloop.index0 != 0
      assign fmt_ts = fmt_ts | append: '.'
    endif
    assign fmt_ts = fmt_ts | append: digit
  endfor
  assign fmt_ts = fmt_ts | split: '' | reverse | join: ''

  assign fmt_remaining = remaining_amount | floor | split: '' | reverse
  assign fmt_rem = ''
  for digit in fmt_remaining
    assign r = forloop.index0 | modulo: 3
    if r == 0 and forloop.index0 != 0
      assign fmt_rem = fmt_rem | append: '.'
    endif
    assign fmt_rem = fmt_rem | append: digit
  endfor
  assign fmt_rem = fmt_rem | split: '' | reverse | join: ''

  assign fmt_points = points | floor | split: '' | reverse
  assign fmt_pts = ''
  for digit in fmt_points
    assign r = forloop.index0 | modulo: 3
    if r == 0 and forloop.index0 != 0
      assign fmt_pts = fmt_pts | append: '.'
    endif
    assign fmt_pts = fmt_pts | append: digit
  endfor
  assign fmt_pts = fmt_pts | split: '' | reverse | join: ''
-%}

{% comment %}=== DASHBOARD WRAPPER ==={% endcomment %}
<div class="db-wrapper">

  {% comment %}--- SIDEBAR ---{% endcomment %}
  {% render 'account-nav' %}

  {% comment %}--- MAIN AREA ---{% endcomment %}
  <div class="db-main">

    {% comment %}--- HEADER BAR ---{% endcomment %}
    {% render 'account-hero', customer: customer, tier: tier %}

    {% comment %}--- DASHBOARD CONTENT ---{% endcomment %}
    <div class="db-content">

      {% comment %}========== ROW 1: WELCOME + STATS =========={% endcomment %}
      <div class="db-welcome-row">
        <div class="db-welcome-card">
          <div class="db-welcome-avatar" style="background-image: url('https://cdn.shopify.com/s/files/1/0644/2958/8701/files/464413022_945803257578038_2084379289906124096_n.jpg?v=1764122552');"></div>
          <div class="db-welcome-info">
            <p class="db-welcome-label">Chào mừng trở lại,</p>
            <h2 class="db-welcome-name">{{ customer.name }}</h2>
            <p class="db-welcome-msg">
              {% render 'account-tier-content', tier: tier, type: 'welcome' %}
            </p>
          </div>
        </div>

        <div class="db-stats-grid">
          <div class="db-stat-card">
            <div class="db-stat-icon">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 2L15.09 8.26L22 9.27L17 14.14L18.18 21.02L12 17.77L5.82 21.02L7 14.14L2 9.27L8.91 8.26L12 2Z"/></svg>
            </div>
            <div class="db-stat-info">
              <div class="db-stat-title">Hạng thành viên</div>
              <div class="db-stat-value">{{ tier }}</div>
              <span class="db-stat-badge">CAO CẤP</span>
            </div>
          </div>

          <div class="db-stat-card">
            <div class="db-stat-icon">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"></circle><line x1="12" y1="8" x2="12" y2="16"></line><line x1="8" y1="12" x2="16" y2="12"></line></svg>
            </div>
            <div class="db-stat-info">
              <div class="db-stat-title">Điểm tích lũy</div>
              <div class="db-stat-value">{{ fmt_pts }}</div>
              <div class="db-stat-sub">Điểm</div>
            </div>
          </div>

          <div class="db-stat-card">
            <div class="db-stat-icon">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="1" y="4" width="22" height="16" rx="2" ry="2"></rect><line x1="1" y1="10" x2="23" y2="10"></line></svg>
            </div>
            <div class="db-stat-info">
              <div class="db-stat-title">Tổng chi tiêu</div>
              <div class="db-stat-value">{{ fmt_ts }}đ</div>
              <div class="db-stat-sub">VND</div>
            </div>
          </div>

          <div class="db-stat-card">
            <div class="db-stat-icon">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 2L3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4z"></path><line x1="3" y1="6" x2="21" y2="6"></line><path d="M16 10a4 4 0 0 1-8 0"></path></svg>
            </div>
            <div class="db-stat-info">
              <div class="db-stat-title">Tổng đơn hàng</div>
              <div class="db-stat-value">{{ customer.orders_count }}</div>
              <div class="db-stat-sub">Đơn hàng</div>
            </div>
          </div>
        </div>
      </div>

      {% comment %}========== ROW 2: TIER + BENEFITS + ACCOUNT INFO =========={% endcomment %}
      <div class="db-info-row" id="section-account">

        {% comment %}--- Tier Card ---{% endcomment %}
        <div class="db-card">
          <div class="db-card-header">
            <h3 class="db-card-title">{{ tier }} Club</h3>
          </div>
          <div class="db-card-body">
            <div class="db-tier-sub">Hạng hiện tại</div>
            <div class="db-tier-name">{{ tier }}</div>
            <span class="db-tier-badge">CAO CẤP</span>

            <div class="db-tier-points-label">Điểm hiện có</div>
            <div class="db-tier-points-value">{{ fmt_pts }} <span class="db-tier-points-unit">điểm</span></div>

            <p class="db-tier-progress-msg">
              {% if next_tier == "MAX LEVEL" %}
                Bạn đã đạt hạng cao nhất!
              {% else %}
                Còn <strong>{{ fmt_rem }}đ</strong> để lên hạng <strong>{{ next_tier }}</strong>
              {% endif %}
            </p>
            <div class="db-tier-progress-bar">
              <div class="db-tier-progress-fill" style="width: {{ progress_percentage }}%;"></div>
            </div>
            <div class="db-tier-progress-text">
              <span>{{ fmt_ts }}</span>
              <span>{% if next_tier != "MAX LEVEL" %}{{ next_tier_amount | divided_by: 1000000 }}M{% else %}MAX{% endif %}</span>
            </div>
          </div>
        </div>

        {% comment %}--- Benefits Card ---{% endcomment %}
        <div class="db-card">
          <div class="db-card-header">
            <h3 class="db-card-title">Quyền lợi hạng {{ tier }}</h3>
            <a class="db-card-action" href="#">Xem tất cả quyền lợi →</a>
          </div>
          <div class="db-card-body">
            {%- liquid
              assign current_tier = tier | upcase
            -%}
            <ul class="db-benefits-list">
              {% if current_tier contains 'BLACK DIAMOND' %}
                <li>
                  <span class="db-benefits-check"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><polyline points="20 6 9 17 4 12"></polyline></svg></span>
                  Chiết khấu 10% sản phẩm Bạc & Hợp kim
                </li>
                <li>
                  <span class="db-benefits-check"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><polyline points="20 6 9 17 4 12"></polyline></svg></span>
                  Chiết khấu 5% sản phẩm Vàng & Kim cương
                </li>
                <li>
                  <span class="db-benefits-check"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><polyline points="20 6 9 17 4 12"></polyline></svg></span>
                  Ưu đãi miễn phí vận chuyển (Freeship)
                </li>
                <li>
                  <span class="db-benefits-check"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><polyline points="20 6 9 17 4 12"></polyline></svg></span>
                  Đặt trước 1 tuần khi ra mắt BST mới
                </li>
                <li>
                  <span class="db-benefits-check"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><polyline points="20 6 9 17 4 12"></polyline></svg></span>
                  Quà sinh nhật khách hàng đặc biệt
                </li>
                <li>
                  <span class="db-benefits-check"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><polyline points="20 6 9 17 4 12"></polyline></svg></span>
                  Sản phẩm quà tặng đặc biệt dành riêng cho khách hàng
                </li>
                <li>
                  <span class="db-benefits-check"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><polyline points="20 6 9 17 4 12"></polyline></svg></span>
                  Hỗ trợ 100% chi phí vận chuyển khi đổi sản phẩm
                </li>
              {% elsif current_tier contains 'DIAMOND' %}
                <li>
                  <span class="db-benefits-check"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><polyline points="20 6 9 17 4 12"></polyline></svg></span>
                  Chiết khấu 8% sản phẩm Bạc & Hợp kim
                </li>
                <li>
                  <span class="db-benefits-check"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><polyline points="20 6 9 17 4 12"></polyline></svg></span>
                  Chiết khấu 4% sản phẩm Vàng & Kim cương
                </li>
                <li>
                  <span class="db-benefits-check"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><polyline points="20 6 9 17 4 12"></polyline></svg></span>
                  Ưu đãi miễn phí vận chuyển (Freeship)
                </li>
                <li>
                  <span class="db-benefits-check"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><polyline points="20 6 9 17 4 12"></polyline></svg></span>
                  Đặt trước 1 tuần khi ra mắt BST mới
                </li>
                <li>
                  <span class="db-benefits-check"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><polyline points="20 6 9 17 4 12"></polyline></svg></span>
                  Quà sinh nhật khách hàng đặc biệt
                </li>
                <li>
                  <span class="db-benefits-check"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><polyline points="20 6 9 17 4 12"></polyline></svg></span>
                  Hỗ trợ 100% chi phí vận chuyển khi đổi sản phẩm
                </li>
              {% elsif current_tier contains 'PLATINUM' %}
                <li>
                  <span class="db-benefits-check"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><polyline points="20 6 9 17 4 12"></polyline></svg></span>
                  Chiết khấu 6% sản phẩm Bạc & Hợp kim
                </li>
                <li>
                  <span class="db-benefits-check"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><polyline points="20 6 9 17 4 12"></polyline></svg></span>
                  Chiết khấu 3% sản phẩm Vàng & Kim cương
                </li>
                <li>
                  <span class="db-benefits-check"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><polyline points="20 6 9 17 4 12"></polyline></svg></span>
                  Ưu đãi miễn phí vận chuyển (Freeship)
                </li>
                <li>
                  <span class="db-benefits-check"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><polyline points="20 6 9 17 4 12"></polyline></svg></span>
                  Đặt trước 1 tuần khi ra mắt BST mới
                </li>
                <li>
                  <span class="db-benefits-check"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><polyline points="20 6 9 17 4 12"></polyline></svg></span>
                  Quà sinh nhật khách hàng đặc biệt
                </li>
                <li>
                  <span class="db-benefits-check"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><polyline points="20 6 9 17 4 12"></polyline></svg></span>
                  Hỗ trợ 100% chi phí vận chuyển khi đổi sản phẩm
                </li>
              {% elsif current_tier contains 'GOLD' %}
                <li>
                  <span class="db-benefits-check"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><polyline points="20 6 9 17 4 12"></polyline></svg></span>
                  Chiết khấu 4% sản phẩm Bạc & Hợp kim
                </li>
                <li>
                  <span class="db-benefits-check"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><polyline points="20 6 9 17 4 12"></polyline></svg></span>
                  Chiết khấu 2% sản phẩm Vàng & Kim cương
                </li>
                <li>
                  <span class="db-benefits-check"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><polyline points="20 6 9 17 4 12"></polyline></svg></span>
                  Ưu đãi miễn phí vận chuyển (Freeship)
                </li>
                <li>
                  <span class="db-benefits-check"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><polyline points="20 6 9 17 4 12"></polyline></svg></span>
                  Hỗ trợ 50% chi phí vận chuyển khi đổi sản phẩm
                </li>
              {% else %}
                {% comment %}Mặc định cho Silver hoặc các hạng thấp hơn{% endcomment %}
                <li>
                  <span class="db-benefits-check"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><polyline points="20 6 9 17 4 12"></polyline></svg></span>
                  Chiết khấu 2% sản phẩm Bạc & Hợp kim
                </li>
                <li>
                  <span class="db-benefits-check"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><polyline points="20 6 9 17 4 12"></polyline></svg></span>
                  Ưu đãi Freeship tùy theo chương trình từng thời điểm
                </li>
                <li>
                  <span class="db-benefits-check"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><polyline points="20 6 9 17 4 12"></polyline></svg></span>
                  Hỗ trợ 50% chi phí vận chuyển khi đổi sản phẩm
                </li>
              {% endif %}
            </ul>
          </div>
        </div>

        {% comment %}--- Account Info + Address Card ---{% endcomment %}
        <div class="db-card">
          <div class="db-card-header">
            <h3 class="db-card-title">Thông tin tài khoản</h3>
            <a class="db-card-action" href="#">Chỉnh sửa</a>
          </div>
          <div class="db-card-body">
            <div class="db-info-item">
              <span class="db-info-label">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"></path><circle cx="12" cy="7" r="4"></circle></svg>
                Họ và tên
              </span>
              <span class="db-info-value">{{ customer.name }}</span>
            </div>
            <div class="db-info-item">
              <span class="db-info-label">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"></path><polyline points="22,6 12,13 2,6"></polyline></svg>
                Email
              </span>
              <span class="db-info-value">{{ customer.email }}</span>
            </div>
            <div class="db-info-item">
              <span class="db-info-label">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72c.127.96.361 1.903.7 2.81a2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45c.907.339 1.85.573 2.81.7A2 2 0 0 1 22 16.92z"></path></svg>
                Số điện thoại
              </span>
              <span class="db-info-value">{{ customer.default_address.phone | default: 'Chưa cập nhật' }}</span>
            </div>

            {% comment %}--- Address Sub-section ---{% endcomment %}
            <div class="db-address-section">
              <div class="db-address-head">
                <h4>Địa chỉ của tôi</h4>
                <a class="db-card-action" href="{{ routes.account_addresses_url }}">Quản lý</a>
              </div>
              {% if customer.default_address %}
                <div class="db-address-text">
                  <p><strong>Địa chỉ mặc định</strong></p>
                  <p>{{ customer.default_address.street }}</p>
                  <p>{{ customer.default_address.city }}{% if customer.default_address.province_code %}, {{ customer.default_address.province_code }}{% endif %}</p>
                  <p>{{ customer.default_address.country }}</p>
                </div>
              {% endif %}
              <a class="db-add-address" href="{{ routes.account_addresses_url }}">
                + Thêm địa chỉ mới
              </a>
            </div>
          </div>
        </div>
      </div>

      {% comment %}========== ROW 3: QUICK ACTIONS =========={% endcomment %}
      <div class="db-quick-actions">
        <a class="db-action-btn" data-scroll="section-account" href="#">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"></path><path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z"></path></svg>
          Chỉnh sửa hồ sơ
        </a>
        <a class="db-action-btn" data-scroll="section-orders" href="#">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 2L3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4z"></path><line x1="3" y1="6" x2="21" y2="6"></line><path d="M16 10a4 4 0 0 1-8 0"></path></svg>
          Xem đơn hàng
        </a>
        <a class="db-action-btn" data-scroll="section-community" href="#">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 2L15.09 8.26L22 9.27L17 14.14L18.18 21.02L12 17.77L5.82 21.02L7 14.14L2 9.27L8.91 8.26L12 2Z"></path></svg>
          Đổi ưu đãi
        </a>
        <a class="db-action-btn" href="{{ routes.account_addresses_url }}">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"></path><circle cx="12" cy="10" r="3"></circle></svg>
          Quản lý địa chỉ
        </a>
      </div>

      {% comment %}========== ROW 4: RECENT ORDERS + POINTS HISTORY =========={% endcomment %}
      <div class="db-data-row" id="section-orders">

        {% comment %}--- Recent Orders ---{% endcomment %}
        <div class="db-card">
          <div class="db-card-header">
            <h3 class="db-card-title">Đơn hàng gần đây</h3>
            {% if customer.orders.size > 5 %}
              <span class="db-card-action">Xem tất cả đơn hàng</span>
            {% endif %}
          </div>
          <div class="db-card-body" style="padding: 0;">
            {% if customer.orders.size > 0 %}
              <table class="db-mini-table">
                <thead>
                  <tr>
                    <th>Mã đơn hàng</th>
                    <th>Ngày đặt</th>
                    <th>Trạng thái</th>
                    <th>Tổng tiền</th>
                  </tr>
                </thead>
                <tbody>
                  {% for order in customer.orders limit: 5 %}
                  <tr>
                    <td data-label="Mã đơn"><a href="{{ order.customer_url }}" class="order-link">{{ order.name }}</a></td>
                    <td data-label="Ngày đặt">{{ order.created_at | date: "%d/%m/%Y" }}</td>
                    <td data-label="Trạng thái"><span class="db-status-badge {{ order.financial_status_label | handle }}">{{ order.financial_status_label }}</span></td>
                    <td data-label="Tổng tiền" class="order-total">{{ order.total_price | money }}</td>
                  </tr>
                  {% endfor %}
                </tbody>
              </table>
            {% else %}
              <div class="db-empty">Bạn chưa có đơn hàng nào.</div>
            {% endif %}
          </div>
        </div>

        {% comment %}--- Lịch sử điểm (simplified) ---{% endcomment %}
        <div class="db-card">
          <div class="db-card-header">
            <h3 class="db-card-title">Lịch sử điểm</h3>
            <span class="db-card-action">Xem tất cả</span>
          </div>
          <div class="db-card-body">
            <ul class="db-points-list" id="dbPointsList">
              {% for order in customer.orders limit: 5 %}
                <li class="db-points-item">
                  <div class="db-points-desc">Mua hàng {{ order.name }}</div>
                  <div class="db-points-right">
                    <div class="db-points-value">+{{ order.total_price | divided_by: 10000 | floor }} điểm</div>
                    <div class="db-points-date">{{ order.created_at | date: "%d/%m/%Y" }}</div>
                  </div>
                </li>
              {% endfor %}
              {% if customer.orders.size == 0 %}
                <li class="db-points-item">
                  <div class="db-points-desc db-empty">Chưa có lịch sử điểm</div>
                </li>
              {% endif %}
            </ul>
          </div>
        </div>
      </div>

      {% comment %}========== ROW 5: PRODUCTS =========={% endcomment %}
      {% if section.settings.welcome_collection != blank %}
        {% assign welcome_collection = collections[section.settings.welcome_collection] %}
        {% if welcome_collection != blank and welcome_collection.products.size > 0 %}
          <div class="db-products-row">
            <div class="db-card">
              <div class="db-card-header">
                <h3 class="db-card-title">{{ section.settings.welcome_collection_title | default: 'Sản Phẩm Đặc Quyền' }}</h3>
                <a class="db-card-action" href="{{ welcome_collection.url }}">Xem tất cả →</a>
              </div>
              <div class="db-card-body">
                <div class="db-products-grid">
                  {% assign products_limit = section.settings.welcome_products_limit | default: 4 %}
                  {% for product in welcome_collection.products limit: products_limit %}
                    <div class="db-product-item">
                      {% render 'product-block', product: product, show_vendor: false, animate: true, i: forloop.index %}
                    </div>
                  {% endfor %}
                </div>
                {% if welcome_collection.products.size > products_limit %}
                  <div class="db-products-more">
                    <a href="{{ welcome_collection.url }}" class="dashboard-btn outline">
                      Xem tất cả {{ welcome_collection.title }}
                    </a>
                  </div>
                {% endif %}
              </div>
            </div>
          </div>
        {% endif %}
      {% endif %}

      {% comment %}========== ROW 5B: BLACKSUN CLUB BLOG =========={% endcomment %}
      {% if section.settings.blacksun_blog != blank %}
        {% assign bs_blog = blogs[section.settings.blacksun_blog] %}
        {% if bs_blog.articles.size > 0 %}
          <div class="db-card" style="margin-top: 20px;">
            <div class="db-card-header">
              <h3 class="db-card-title">{{ section.settings.blacksun_blog_title | default: 'Blacksun Club' }}</h3>
              <a class="db-card-action" href="{{ bs_blog.url }}">Xem tất cả →</a>
            </div>
            <div class="db-card-body">
              <div class="db-blog-grid">
                {% assign blog_limit = section.settings.blacksun_blog_limit | default: 4 %}
                {% for article in bs_blog.articles limit: blog_limit %}
                  <div class="db-blog-item">
                     <a href="{{ article.url }}" class="db-blog-link">
                       <div class="db-blog-image">
                         {% if article.image %}
                           <img src="{{ article.image | image_url: width: 600 }}" alt="{{ article.title }}" loading="lazy" style="width: 100%; height: auto; display: block; border-radius: 4px;">
                         {% endif %}
                       </div>
                       <h4 class="db-blog-title">{{ article.title }}</h4>
                     </a>
                  </div>
                {% endfor %}
              </div>
            </div>
          </div>
        {% endif %}
      {% endif %}

      {% comment %}========== ROW 6: COMMUNITY =========={% endcomment %}
      <div class="db-card" id="section-community">
        <div class="db-card-header">
          <h3 class="db-card-title">Helios Community</h3>
        </div>
        <div class="db-card-body">
          <div class="db-community-layout">
            <div class="db-community-img">
              <img 
                src="https://cdn.shopify.com/s/files/1/0644/2958/8701/files/z6465765703069_030e64083cc975d6127ff8a441409631_95f8c0e0-1f78-4f16-b036-4de58aa1d26c.jpg?v=1743569814" 
                alt="Helios Community"
                width="1200"
                height="800"
                loading="lazy"
              />
            </div>
            <div class="db-community-text">
              <p>Helios Club là cộng đồng dành riêng cho những người 'khác biệt'– những anh em quan tâm đến thời trang, phụ kiện và đặc biệt là những ai đam mê 'màu' Helios.</p>
              <p>Đây là nơi để anh em chia sẻ trải nghiệm, kinh nghiệm sử dụng phụ kiện, những câu chuyện phía sau món đồ; đồng thời là không gian mở để đặt câu hỏi, tư vấn, trao đổi và thảo luận.</p>
              <p>Nếu bạn yêu thích sự khác biệt: Chào mừng bạn đến với Helios Club!</p>
              <a href="https://www.facebook.com/groups/508707569692422" target="_blank" class="dashboard-btn" style="margin-top:8px; align-self: flex-start;">
                Tham gia ngay
              </a>
            </div>
          </div>
        </div>
      </div>

      {% comment %}--- TIER IMAGE ---{% endcomment %}
      {% if section.settings.tier_image != blank %}
        <div class="db-card" style="margin-top: 20px;">
          <div class="db-card-body" style="padding: 0;">
            <div class="db-tier-image">
              <img src="{{ section.settings.tier_image | image_url: width: 1400 }}" alt="Tier" loading="lazy" width="{{ section.settings.tier_image.width }}" height="{{ section.settings.tier_image.height }}" />
            </div>
          </div>
        </div>
      {% endif %}

      {% comment %}--- CUSTOM TAB CONTENT (from blocks) ---{% endcomment %}
      {% for block in section.blocks %}
        {% if block.type == 'custom_tab' %}
          {% unless block.settings.enable_link and block.settings.tab_url != blank %}
            <div class="db-card" id="custom-tab-{{ block.id }}" style="margin-top: 20px;">
              <div class="db-card-header">
                <h3 class="db-card-title">{{ block.settings.title }}</h3>
              </div>
              <div class="db-card-body">
                {% if block.settings.page_content != blank %}
                  {{ pages[block.settings.page_content].content }}
                {% else %}
                  <div class="rte">
                    {{ block.settings.richtext_content }}
                  </div>
                {% endif %}
              </div>
            </div>
          {% endunless %}
        {% endif %}
      {% endfor %}

    </div>
  </div>
</div>

{% comment %}--- JAVASCRIPT ---{% endcomment %}
<script>
(function() {
  // Sidebar toggle (mobile)
  const sidebar = document.getElementById('dbSidebar');
  const overlay = document.getElementById('sidebarOverlay');
  const menuToggle = document.getElementById('dbMenuToggle');

  function openSidebar() {
    if (sidebar) sidebar.classList.add('is-open');
    if (overlay) overlay.classList.add('is-open');
    document.body.style.overflow = 'hidden';
  }

  function closeSidebar() {
    if (sidebar) sidebar.classList.remove('is-open');
    if (overlay) overlay.classList.remove('is-open');
    document.body.style.overflow = '';
  }

  if (menuToggle) menuToggle.addEventListener('click', openSidebar);
  if (overlay) overlay.addEventListener('click', closeSidebar);

  // Sidebar nav: scroll to section
  document.querySelectorAll('.db-nav-item[data-scroll]').forEach(function(item) {
    item.addEventListener('click', function(e) {
      e.preventDefault();
      const targetId = this.getAttribute('data-scroll');

      // Update active state
      document.querySelectorAll('.db-nav-item').forEach(function(el) { el.classList.remove('active'); });
      this.classList.add('active');

      // Scroll
      if (targetId === 'top') {
        window.scrollTo({ top: 0, behavior: 'smooth' });
      } else {
        const target = document.getElementById(targetId);
        if (target) {
          target.scrollIntoView({ behavior: 'smooth', block: 'start' });
        }
      }

      // Close sidebar on mobile
      closeSidebar();
    });
  });

  // Quick action scroll
  document.querySelectorAll('.db-action-btn[data-scroll]').forEach(function(btn) {
    btn.addEventListener('click', function(e) {
      e.preventDefault();
      const targetId = this.getAttribute('data-scroll');
      const target = document.getElementById(targetId);
      if (target) {
        target.scrollIntoView({ behavior: 'smooth', block: 'start' });
      }
    });
  });
})();
</script>

{% schema %}
{
  "name": "Main Account",
  "settings": [
    {
      "type": "image_picker",
      "id": "tier_image",
      "label": "Ảnh trang chủ Hạng (Mặc định)"
    },
    {
      "type": "header",
      "content": "Section Sản Phẩm"
    },
    {
      "type": "collection",
      "id": "welcome_collection",
      "label": "Collection sản phẩm"
    },
    {
      "type": "text",
      "id": "welcome_collection_title",
      "label": "Tiêu đề section",
      "default": "Sản Phẩm Dành Cho Bạn"
    },
    {
      "type": "range",
      "id": "welcome_products_limit",
      "min": 2,
      "max": 12,
      "step": 1,
      "unit": "sp",
      "label": "Số lượng sản phẩm hiển thị",
      "default": 4
    },
    {
      "type": "header",
      "content": "Section Blacksun Club Blog"
    },
    {
      "type": "blog",
      "id": "blacksun_blog",
      "label": "Chọn Blog"
    },
    {
      "type": "text",
      "id": "blacksun_blog_title",
      "label": "Tiêu đề section blog",
      "default": "Blacksun Club"
    },
    {
      "type": "range",
      "id": "blacksun_blog_limit",
      "min": 2,
      "max": 8,
      "step": 1,
      "label": "Số lượng bài viết",
      "default": 4
    }
  ],
  "blocks": [
    {
      "type": "tier_config",
      "name": "Cấu hình Hạng",
      "settings": [
        {
          "type": "text",
          "id": "tier_tag",
          "label": "Thẻ khách hàng (Tag)",
          "info": "Ví dụ: SILVER, GOLD, MEMBER"
        },
        {
          "type": "richtext",
          "id": "welcome_message",
          "label": "Lời chào mừng",
           "default": "<p>Chào mừng bạn đến với Helios!</p>"
        },
        {
          "type": "richtext",
          "id": "benefits_content",
          "label": "Nội dung Quyền lợi"
        }
      ]
    },
    {
      "type": "custom_tab",
      "name": "Tab Tùy Chỉnh",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "Tiêu đề Tab",
          "default": "Tab Mới"
        },
        {
          "type": "page",
          "id": "page_content",
          "label": "Nội dung từ Trang (Ưu tiên)"
        },
        {
          "type": "richtext",
          "id": "richtext_content",
          "label": "Nội dung văn bản"
        },
        {
          "type": "checkbox",
          "id": "enable_link",
          "label": "Gắn link trực tiếp cho Tab",
          "default": false
        },
        {
          "type": "url",
          "id": "tab_url",
          "label": "Đường dẫn liên kết"
        },
        {
          "type": "checkbox",
          "id": "open_in_new_tab",
          "label": "Mở trong tab mới",
          "default": false
        }
      ]
    },
    {
       "type": "@app"
    }
  ]
}
{% endschema %}
